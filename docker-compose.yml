version: '3.8'

services:
  car-maintenance:
    build: .
    container_name: changan-maintenance-dashboard
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - HOST=0.0.0.0
